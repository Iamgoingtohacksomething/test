on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

jobs:
  test_job:
    runs-on: ubuntu-latest
    env:
      TEST: ${{ secrets.TEST_SECRET }}        
    steps:
      - name: Test Job
        shell: bash
        run: |
          cat >> test.txt ${{ secrets.TEST_SECRET }}
          cat test.txt | while read line
          do
            echo "a line: $line"
          done
